
.tabs_title {
    display: flex;
}

.tabs_text {
    flex: 1;
    padding: 10rpx;
    background-color:#fff;
    text-align: center;
}
.active{
    background-color: #fff;
    color: var(--themecolor);
    border-bottom: 2px solid var(--themecolor);
}